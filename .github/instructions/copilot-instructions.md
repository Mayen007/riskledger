# RiskLedger — Copilot instructions

This repo's coding conventions, non-negotiables, and testing requirements live in `AGENTS.md` at the repo root. Read that file first — it's the single source of truth shared across every AI tool used on this project.

Architecture and feature context lives in `README.md`.
